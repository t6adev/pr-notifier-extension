name: Create Release PR

on:
  push:
    branches: [main]

# Skip if triggered by the bot's own version bump commit
jobs:
  create-release-pr:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Bump patch version
        id: version
        run: |
          node --strip-types scripts/bump-version.ts patch
          VERSION=$(node -p "JSON.parse(require('fs').readFileSync('manifest.json','utf8')).version")
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Commit and push version bump
        env:
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add manifest.json
          git commit -m "chore: bump version to ${VERSION}"
          git push origin main

      - name: Create or update release PR
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          PR_NUMBER=$(gh pr list --base release --head main --state open --json number --jq '.[0].number // empty')
          if [ -z "$PR_NUMBER" ]; then
            gh pr create \
              --base release \
              --head main \
              --title "Release v${VERSION}" \
              --body "Merging this PR will publish **v${VERSION}**."
          else
            gh pr edit "$PR_NUMBER" \
              --title "Release v${VERSION}" \
              --body "Merging this PR will publish **v${VERSION}**."
          fi
